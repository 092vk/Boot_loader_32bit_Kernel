# Boot_loader_for_32bit_Kernel

![Title](./ascii-art.png)

## Tools required: Install the following tools
```bash
    sudo apt install nasm
    sudo apt install qemu-system-x86
    sudo apt install bless


